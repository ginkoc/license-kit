server:
    port: 8686

spring:
    redis:
      host: localhost
      port: 6379
      password: mypass
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0